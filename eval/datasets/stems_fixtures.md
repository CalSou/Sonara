# Stem eval fixtures (skeleton)

Eval runners synthesise a simple sine buffer at runtime (no committed WAV/FLAC binaries).

**V2:** Add programmatic mixes with known ground-truth stems for SI-SDR thresholds against real separators.
