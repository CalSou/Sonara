/**
 * Tempo estimation from audio (skeleton).
 *
 * **V2:** onset envelope + autocorrelation or beat-tracker.
 */
export function estimateBpmAutocorr(_buffer: AudioBuffer): number | null {
  return null;
}
