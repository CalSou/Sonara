/**
 * Spectral descriptors (skeleton).
 *
 * **V2:** FFT-based centroid, rolloff, MFCC hooks (consider `fft.js` devDependency).
 */
export function spectralCentroidStub(_buffer: AudioBuffer): null {
  return null;
}
